# ============================================================
# ASC to CSV 依赖清单
# ============================================================
# Python版本要求: >= 3.8
# 安装命令: pip install -r requirements.txt
# ============================================================

# 核心依赖
cantools>=37.0.0,<42.0.0

# 打包工具（仅开发时需要）
pyinstaller>=5.0.0,<7.0.0
